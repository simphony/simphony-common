@startuml

class H5Lattice as "H5Lattice(Group)" {
  _v_name: String = <name>
  lattice : Table = Nodes
  data : Table = Data
  -- Node Attributes --
  type: String[20]  
  base_vect : Double[2..3]
  size : Int[2..3]
  origin: Double[2..3]
}

class Nodes as "Nodes(Table)" {
  _v_name: String = "lattice"
  row : Record
}
note left : index into the lattice node table is calculated using numpy....

class Data as "Data(Table)" {
  _v_name: String = "data"
  row : NoUidRecord
}

class Record {
  [ uid ]: String[32]
  [ data ]: DataRow
  [ mask ]: Bool[size of CUBARow]    
}

class NoUidRecord {
  [ data ]: DataRow
  [ mask ]: Bool[size of CUBARow]    
}

class DataRow
note left: DataRow is a Row where the columns are CUBA key names.

H5Lattice -- Data
Data -- Record
Nodes -- NoUidRecord
Record -- DataRow
NoUidRecord -- DataRow

@enduml